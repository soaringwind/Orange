#include <stdio.h>
#include <iostream>
#include <fstream>
#include <vector>
#include <deque>
#include <algorithm>
#include "boost/version.hpp"

using namespace std;
int main(void)
{
   cout << BOOST_VERSION << endl;
   // vector<int> v1;
   // v1.emplace_back(1);
   // v1.emplace_back(1);
   // vector<int>::iterator it = v1.end();
   // cout << *it << endl;
   // deque<int> d1;
   return 0;
}